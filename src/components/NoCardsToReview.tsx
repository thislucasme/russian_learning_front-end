import { Text, VStack } from "@chakra-ui/react";

export const NoCardsToReview = () =>{
    return (
        <VStack>
            <Text>No cards to review today</Text>
        </VStack>
    );
}